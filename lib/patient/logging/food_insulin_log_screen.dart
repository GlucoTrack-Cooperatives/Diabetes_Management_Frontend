import 'package:flutter/material.dart';

class FoodInsulinLogScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Food & Insulin Log'),
      ),
      body: Center(
        child: Text('Food & Insulin Log Content Goes Here'),
      ),
    );
  }
}
