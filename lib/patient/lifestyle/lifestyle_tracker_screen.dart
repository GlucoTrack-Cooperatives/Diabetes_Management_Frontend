import 'package:flutter/material.dart';

class LifestyleTrackerScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Lifestyle Tracker'),
      ),
      body: Center(
        child: Text('Lifestyle Tracker Content Goes Here'),
      ),
    );
  }
}
