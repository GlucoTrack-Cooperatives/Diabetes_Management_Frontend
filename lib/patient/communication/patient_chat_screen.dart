import 'package:flutter/material.dart';

class PatientChatScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Chat with Physician'),
      ),
      body: Center(
        child: Text('Patient Chat Content Goes Here'),
      ),
    );
  }
}
