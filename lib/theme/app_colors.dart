import 'package:flutter/material.dart';

class AppColors {
  // Soft Pop / Cozy UI Palette
  static const Color mint = Color(0xFFE2F3E9);
  static const Color skyBlue = Color(0xFFE3F2FD);
  static const Color peach = Color(0xFFFFF0E0);
  static const Color lavender = Color(0xFFF3E5F5);
  
  static const Color primary = Color(0xFF4DB6AC); // A softer teal
  static const Color secondary = Color(0xFF64B5F6); // A softer blue
  static const Color background = Color(0xFFF8F9FE);
  static const Color surface = Colors.white;
  static const Color error = Color(0xFFFF8A80);

  static const Color textPrimary = Color(0xFF37474F); // Soft dark slate
  static const Color textSecondary = Color(0xFF78909C);
  
  // Soft Shadows
  static Color shadow = Colors.black.withOpacity(0.05);
  static const Color border = Color(0xFFECEFF1); // Very light border
}
